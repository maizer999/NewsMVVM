# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NewsAssessment' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'Alamofire', '~> 4.8.2'
  pod 'SDWebImage', '~> 4.0'
  pod 'PKHUD', '~> 5.0'
  pod 'SimpleTwoWayBinding'
  # Pods for NewsAssessment
  
  target 'NewsAssessmentTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  target 'NewsAssessmentUITests' do
    # Pods for testing
  end
  
end
